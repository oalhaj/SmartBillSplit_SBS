name: splitsnap
version: 0.1.0
publish_to: none

description: SplitSnap - offline bill splitting app.

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  image_picker: ^1.1.2
  image_cropper: ^5.0.1
  google_mlkit_text_recognition: ^0.13.0
  flutter_contacts: ^1.1.9+2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3
  url_launcher: ^6.3.0
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
